.main {
  position: fixed;
  width: 100vw;
  height: 100vh;
  background: #fff;
  z-index: 1;
}

.main-bg-item {
  position: absolute;
  left: 0;
  right: 0;
  width: 750rpx;
  z-index: 2;
}

.main-bg-top {
  top: 0;
}

.main-bg-bottom {
  top: 1030rpx;
}

.main-view {
  position: relative;
  padding-top: 200rpx;
  z-index: 3;
}

.swiperClass {
  margin: 0;
  height: 641rpx;
}

.swiper-item-view {
  position: relative;
  z-index: 1;
}

.slide-image {
  width: 427rpx;
  height: 641rpx;
  border-radius: 40rpx;
  position: relative;
  z-index: 4;
}

image.active {
  transform: none;
  transition: all 0.4s ease-in 0s;
}

image.firstitem {
  transform: scale(0.41493) translateX(-80rpx);
  box-shadow:0rpx 9rpx 30rpx 8rpx rgba(80,84,88,0.34);
}

image.leftitem {
  transform: scale(0.41493) translateX(80rpx);
  box-shadow:0rpx 9rpx 30rpx 8rpx rgba(80,84,88,0.34);
}

image.rightitem {
  transform: scale(0.41493) translateX(-80rpx);
  box-shadow:0rpx 9rpx 30rpx 8rpx rgba(80,84,88,0.34);
}

image.quiet {
  opacity: 0.6;
  transition: all 0.2s ease-in 0s;
}

.bottom-view {
  display: flex;
  align-items: center;
  position: relative;
  z-index: 3;
  flex-direction: column;
}

.bottom-triangle {
  margin-top: 40rpx;
  width: 0;
  height: 0;
  border-width: 16rpx 16rpx;
  border-style: solid;
  border-color: #535353 transparent transparent transparent;
}

.detail-btn {
  margin-top: 40rpx;
}

.border-out-line {
  background: #fff;
}

.border-in-line {
  background: #fff;
}
